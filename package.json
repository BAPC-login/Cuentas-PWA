{
  "name": "cuentas-pwa",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev:api": "wrangler dev backend/src/index.js",
    "deploy:api": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply CUENTAS_DB --local",
    "db:migrate:remote": "wrangler d1 migrations apply CUENTAS_DB --remote"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
